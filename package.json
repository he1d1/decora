{
  "type": "module",
  "name": "decora",
  "scripts": {
    "dev": "esbuild src/server.ts --bundle --platform=neutral --minify --sourcemap --outfile=dist/server.js --servedir=dist"
  },
  "dependencies": {
    "htm": "^3.1.1"
  },
  "devDependencies": {
    "esbuild": "^0.15.1",
    "typescript": "^4.7.4"
  }
}
